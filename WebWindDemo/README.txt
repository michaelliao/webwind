This is a demo app for web wind.

Make sure JDK and Ant is installed.

[Optional] Run 'build.bat' to rebuild the project.

Run 'start_server.bat' to start server. It will start Jetty server and open a browser automatically.

If the browser is not open, try this address: http://localhost:8990/
